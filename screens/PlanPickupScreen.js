import React from 'react';
import { View, Text } from 'react-native';

export default function PlanPickupScreen() {
    return (
        <View>
            <Text>Plan pickup</Text>
        </View>
    );
}